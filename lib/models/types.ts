import { GameType } from '@/store'

export type Model = {
  collectionId: string
  collectionName: string
  created: string
  type: GameType
  id: string
  file: string
  url: string
  name: string
  updated: string
  scale: number
}

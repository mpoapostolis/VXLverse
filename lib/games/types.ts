import { Node, QuestType, Scene } from '@/store'

export type Game = {
  id: 'cotp9uustccq9as'
  nodes: Node[]
  scenes: Scene[]
  quests: QuestType[]
  createdBy: 'mpoapostolis@gmail.com'
  title?: string
  description?: string
}
